{%  if  route != 'admin_observation_new_step_1'
    and route != 'admin_observation_new_step_2'
    and route != 'admin_observation_new_step_3'
    and app.session.get('step') != 4
%}
    <section class="add-obs">
        <a data-toggle="tooltip" data-placement="top" title="Ajouter une observation" data-delay="400" class="btn btn-secondary btn-round d-inline-flex flex-column justify-content-center" href="{{ path('admin_observation_new') }}">
        <svg class="nav-icons text-white" >
            <use xlink:href="{{ asset('images/icons/sprite.svg#addObs')}}">
        </svg>
        </a>
    </section>
{% endif %}