<section class="row d-flex justify-content-center p-5">
    <p id="moreResult-spinner" class="pt-5 m-0 text-center text-primary w-100" style="display:none;">
      <i class="fa fa-spinner fa-spin fa-3x" aria-hidden="true"></i>
    </p>
    <form id="moreResult-button" action="{{ path(route) }}" method="get">
      <button class="btn btn-primary p-2 text-center more-results" type="submit">
        <i class="fa fa-plus pr-2"></i>plus de résultats
      </button>
    </form>
  </section>