<div class="tab-pane fade {% if active|default(false) %}show active{% endif %}" id="{{ id }}Obs" role="tabpanel" aria-labelledby="{{ id }}-tab">
    <div class="row observation">
      <div class="col">
        <h6 class="text-light title">{{ title }} (affichage 1 à 6 de {{observations|length}} enregistrements)</h6>
      </div>
      <div class="col-auto">
        <a href="{{ path('admin_observation_new') }}" class="text-white btn btn-primary mt-0">
          <i class="fa fa-plus"></i>
          Ajouter
        </a>
      </div>
    </div>
    <table class="table table-striped table-light mt-3">
      <thead>
        <tr>
          <th scope="col">Obs ID</th>
          <th scope="col">Lieu</th>
          <th scope="col">Moment</th>
          <th scope="col">Oiseau</th>
          <th scope="col">Raison</th>
          <th scope="col-action"></th>
        </tr>
      </thead>
      <tbody>
        {% if id == 'uncommitted'%}
          {% for obs in observations %}
            {{ include('components/tables/observation/_table_noValid.html.twig')}}
          {% endfor %}
        {% else %}
          {% for obs in observations if obs.status <= -199  %}
            {{ include('components/tables/observation/_table_default.html.twig')}}
          {% endfor %}
        {% endif %}
      </tbody>
    </table>
  </div>